spring:
  application:
    name: configserver
  cloud:
    config:
      server:
        git:
          uri: https://github.com/zeev512/spring-cloud-config-server-repository
          default-label: master
          username: zeev512
          # Use the token instead of password
          password: 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
          # password: Aerospace512
          clone-on-start: true
    fail-fast: true